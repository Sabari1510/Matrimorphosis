# Maintenance Tracker Backend

RESTful API service for the Maintenance Tracker application.

## Technologies
- Node.js & Express
- TypeORM (MySQL)
- Mongoose (MongoDB)
- JWT Authentication
- Multer for File Management

## Setup
1. Install dependencies: `npm install`
2. Configure `.env` based on `.env.example`
3. Start development server: `npm run dev`
